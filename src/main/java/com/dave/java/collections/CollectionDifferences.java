package com.dave.java.collections;

import com.dave.java.utils.CollectionMethodDifferences;

/**
 * List
 *    ArrayList 顺序表，有通过索引随机访问的能力
 *    LinkedList 双链表，插入删除效率高【实现队列和栈】
 * Map
 *   HashMap 映射
 *   TreeMap 有序的键值映射
 *   LinkedHashMap 按出入顺序插入的Map映射
 * Set
 *   HashSet 不允许重复的集合
 *   TreeSet 有序的不重复元素的集合
 *   LinkedHashSet 按插入顺序插入的不重复元素集合
 * Vector HashTable Stack---废弃代码
 *  11-80 -- 13/14
 */
public class CollectionDifferences {
    public static void main(String[] args) {
        CollectionMethodDifferences.main(args);
    }
}
